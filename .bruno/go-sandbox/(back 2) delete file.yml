info:
  name: (back 2) delete file
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8000/file
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
