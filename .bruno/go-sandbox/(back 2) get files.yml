info:
  name: (back 2) get files
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/files
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
