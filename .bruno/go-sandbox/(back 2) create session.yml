info:
  name: (back 2) create session
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/new-session
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
