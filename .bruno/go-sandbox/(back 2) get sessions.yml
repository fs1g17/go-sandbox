info:
  name: (back 2) get sessions
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
